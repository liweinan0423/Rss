<%if notice %><p id="notice"><%= notice %></p> <% end %>

<%= render :partial => 'post', 
           :locals => {:post => @post} %>

<%= link_to 'Edit', edit_channel_post_path(@post.channel, @post) %> |
<%= link_to 'Back', channel_path(@post.channel) %>
